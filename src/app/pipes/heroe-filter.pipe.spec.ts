import { HeroeFilterPipe } from './heroe-filter.pipe';

describe('HeroeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
